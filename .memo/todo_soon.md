# 直近のtodoメモ(2026年07月06日)

## バックエンドtodo

### [重大!!]ファイルをアップロードするたびにファイルが消える問題！！！
ファイルをアップロードするたびにファイルが消える。
おそらくフォルダを作り直してしまっている。
ついでにsrvにデータがマウントされていないかも。

### 同一フォルダへの移動をどういう扱いにするかの決定

### リネーム
`/ \ : * ? " < > |`などOSで問題になる文字を含む名前でフォルダ作成・アップロードができるか(`/ \`は以上文字列判定できたが、`: * ? " < > |`は貫通してその名前になれてしまった。ちなみにダウンロードすると`_`に変換された。ダウンロードはエラーにならないので、名前変更はできるが確認画面を出すとかがいいかもしれない。)

### リネームで拡張子を消せてしまう問題
リネームで拡張子を消せてしまう。
管理上はpngなのに、ダウンロード時に拡張子なしファイルとなってしまう。
フロントエンドはで編集できなくてもいいかもしれない。
それか拡張子も編集できるような機能をサーバ側に入れてもいいかもしれない。
拡張子の扱いをどうするかは要検討

### config化
定数で設定している部分をconfig.tomlで設定できるようにする。

### 既知のゴミ箱バグ3種(むずいので後回し)

### 通常ユーザ作成の方法どうするか検討
ユーザ作成ができるようにするか、管理者がデフォルトパスワードでユーザを作り、ユーザ側でパスワードを変更できるようにするかがいいと思う。

### アップロード制限ファイル数
10MBまで送信できるはずが1MBまでしか送信できない問題。

## フロントエンドtodo


## その他todo
### Makefileの整理
Makefileが現在乱雑になっている。
整理する。

### envで同じDBのURLを2回定義している問題


## phase12で修正しなかったもの
### C1 → 修正済
### C2 → 修正済
### C3 → 修正済
### C4
### M1
### M2
### M3
### M4
### M5
### M6
### M7 → 修正済
### M8
### L1
### L2
### L3
### L4
### L5
### L6













# 直近のtodoメモ(2026年06月28日)

## フロントエンド実装前に必須なもの

☑エラーマッピングの仮定義解消（HTTPステータス・エラーコードが確定しないと、フロントで日本語エラーメッセージに変換できない）
☑Access Token 期限切れ時の自動リフレッシュ（これがないと実用にならない）
☑ストレージ上限チェックの実装

## 並行でも後でもいいもの

☑検索のワイルドカードエスケープ
ゴミ箱の復元バグ2件
☑同一フォルダへの移動の扱い → そのまま同一フォルダに移動され、エラーは起きない
config化











## phase12で修正しなかったもの
## C1 → 修正済
## C2 → 修正済
## C3 → 修正済
## C4
## M1
## M2
## M3
## M4
## M5
## M6
## M7 → 修正済
## M8
## L1
## L2
## L3
## L4
## L5
## L6