[package]
name = "node"
version = "0.1.0"
edition = "2024"

[dependencies]
# workspace内の別クレート
identity = { path = "../identity" }

# workspace共通外部クレート
thiserror = { workspace = true }
serde = { workspace = true }
chrono = { workspace = true }