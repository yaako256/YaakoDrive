# backend/crates/config/Cargo.toml

[package]
name = "config"
version = "0.1.0"
edition = "2024"

[dependencies]
# workspace共通クレート
thiserror = { workspace = true }
serde = { workspace = true }

# 固有クレート
# configの定番クレート
config = "0.14"
# envファイル読込用
dotenvy = "0.15"